{
  "name": "serverless-ecommerce-api-turbo",
  "version": "1.0.0",
  "description": "Production-grade Serverless E-commerce API built with TypeScript, MongoDB, and Serverless Framework",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "rm -rf node_modules && turbo run clean",
    "build:packages": "cd packages/constants && npx tsc --skipLibCheck && cd ../config && npx tsc --skipLibCheck && cd ../utils && npx tsc --skipLibCheck && cd ../logger && npx tsc --skipLibCheck && cd ../errors && npx tsc --skipLibCheck && cd ../validators && npx tsc --skipLibCheck && cd ../db && npx tsc --skipLibCheck && cd ../middlewares && npx tsc --skipLibCheck",
    "setup": "npm install && npm run build:packages"
  },
  "keywords": [
    "serverless",
    "ecommerce",
    "typescript",
    "mongodb",
    "api",
    "monorepo",
    "turbo"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.6",
    "serverless-dotenv-plugin": "^6.0.0",
    "turbo": "^1.8.3",
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@8.19.2",
  "dependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/winston": "^2.4.4",
    "aws-lambda": "^1.0.7",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.3",
    "winston": "^3.19.0",
    "zod": "^3.22.4"
  }
}
